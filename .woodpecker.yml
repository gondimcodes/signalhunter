kind: pipeline
name: test-and-release

steps:
  test:
    image: rust:latest
    commands:
      - cargo test --workspace -j 1
    when:
      event: [push, pull_request]

  build:
    image: rust:latest
    commands:
      - cargo build --release -j 1
      - mkdir -p dist
      - cp target/release/signalhunter dist/signalhunter
      - cp config.toml schema.sql README.md LEIAME.md INSTALL.md INSTALACAO.md LICENSE dist/
      - cd dist
      - tar -czf signalhunter-$${CI_COMMIT_TAG}-x86_64-unknown-linux-gnu.tar.gz signalhunter config.toml schema.sql README.md LEIAME.md INSTALL.md INSTALACAO.md LICENSE
    when:
      event: tag

  publish:
    image: woodpeckerci/plugin-release
    settings:
      api_key:
        from_secret: codeberg_token
      base_url: https://codeberg.org
      files:
        - dist/signalhunter-*.tar.gz
    when:
      event: tag
